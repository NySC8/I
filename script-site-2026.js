(()=>{
const toast=document.getElementById('copyToast');
const showToast=()=>{if(!toast)return;toast.classList.add('show');clearTimeout(window.__idfpToast);window.__idfpToast=setTimeout(()=>toast.classList.remove('show'),1700)};
document.querySelectorAll('[data-copy-url]').forEach(btn=>btn.addEventListener('click',async()=>{const path=btn.dataset.copyUrl;const url=new URL(path,location.href).href.replace(/\/$/,'');try{await navigator.clipboard.writeText(url)}catch{const t=document.createElement('textarea');t.value=url;document.body.appendChild(t);t.select();document.execCommand('copy');t.remove()}showToast()}));
const track=document.querySelector('.track');const cards=[...document.querySelectorAll('.news-card')];const dots=[...document.querySelectorAll('.dot-btn')];const prev=document.querySelector('[data-dir="prev"]');const next=document.querySelector('[data-dir="next"]');const progress=document.querySelector('.progress i');let current=0;let timer;
function render(i,animate=true){if(!track||!cards.length)return;current=Math.max(0,Math.min(i,cards.length-1));const gap=parseFloat(getComputedStyle(track).gap)||0;const card=cards[0];const offset=current*(card.getBoundingClientRect().width+gap);track.style.transition=animate?'transform .65s cubic-bezier(.22,.61,.36,1)':'none';track.style.transform=`translateX(${offset}px)`;cards.forEach((c,n)=>c.classList.toggle('is-active',n===current));dots.forEach((d,n)=>d.classList.toggle('active',n===current));if(prev)prev.disabled=current===0;if(next)next.disabled=current===cards.length-1;if(progress)progress.style.width=((current+1)/cards.length*100)+'%'}
function restart(){clearInterval(timer);if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;timer=setInterval(()=>{render(current+1>=cards.length?0:current+1)},5200)}
prev?.addEventListener('click',()=>{render(current-1);restart()});next?.addEventListener('click',()=>{render(current+1);restart()});dots.forEach((d,i)=>d.addEventListener('click',()=>{render(i);restart()}));
let startX=0;track?.addEventListener('touchstart',e=>{startX=e.touches[0].clientX},{passive:true});track?.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-startX;if(Math.abs(dx)>45){if(dx<0)render(current+1);else render(current-1);restart()}},{passive:true});window.addEventListener('resize',()=>render(current,false));if(track){render(0,false);restart()}
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
})();